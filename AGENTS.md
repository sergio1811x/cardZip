# AI working index

1. Start with `docs/AI_CONTEXT/CURRENT_STATE.md`, then read `docs/AI_CONTEXT/PROJECT_MAP.md`.
2. Do not scan the whole repository without a concrete reason. Search with `rg` before reading files.
3. Load only the skill relevant to the task from `.codex/skills/`.
4. After substantial changes, update `docs/AI_CONTEXT/CURRENT_STATE.md`.
5. Before the final response, run the applicable checks in `docs/AI_CONTEXT/QUALITY_GATES.md`.
